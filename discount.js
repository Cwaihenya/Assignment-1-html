const hasStudentCard = true; // Assign true to the constant
if (hasStudentCard === true) { // If you bring your student ID
  console.log("Student discount applied"); // "Student discount applied" is output to the console
}
